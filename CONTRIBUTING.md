# 기여 가이드

`wpygen`에 기여해 주셔서 감사합니다. 아래 절차를 따라주세요.

## 시작하기

1. 저장소를 포크하고 로컬에 클론합니다.
2. `cargo build`로 빌드가 되는지 확인합니다.
3. 작업 브랜치를 만듭니다.

## 개발 방식

이 프로젝트는 **테스트 우선(TDD)** 방식으로 개발합니다. 개발 규칙과 코드 구조는
[`AGENTS.md`](AGENTS.md)를 반드시 먼저 읽어주세요.

- 기능 변경은 실패하는 테스트를 먼저 작성한 뒤 구현합니다.
- `cargo fmt`와 `cargo clippy --all-targets --locked -- -D warnings`를 통과해야 합니다.
- `cargo test`가 모두 통과해야 합니다.

## 커밋 규칙

- 커밋 메시지는 한국어로 간결하게 작성합니다.
- `Cargo.lock`은 실행 바이너리이므로 커밋합니다.
- `target/`, `.env`, 생성 산출물은 커밋하지 않습니다.

## Pull Request

1. 변경 사항을 커밋하고 브랜치를 푸시합니다.
2. Pull Request를 열고 변경 내용과 테스트 결과를 설명합니다.
3. CI(포맷, 린트, 테스트, 시크릿 스캔)가 모두 통과해야 병합됩니다.

## 이슈

- 버그 리포트: 재현 방법과 예상/실제 동작을 포함해 주세요.
- 기능 제안: 사용 사례와 기대 동작을 설명해 주세요.

보안 취약점은 이슈로 올리지 말고 [`SECURITY.md`](SECURITY.md)의 절차를 따르세요.
